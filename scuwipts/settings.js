cwass SettingsHook extends Hook {
    constuwuctouw() {
        supeuw();
        this.ouwiginaw_muwticats = nuww;
        this.ouwiginaw_uwevoke_window = nuww;
    }

    uwegisteuw() {
        if (this.is_uwegisteuwed) {
            uwetuuwn;
        }
        supeuw.uwegisteuw();
        this.ouwiginaw_muwticats = MODUwES.SEuwVEuw_PuwOPS.MUwTICAST_wIMIT_GwOBAw;
        MODUwES.SEuwVEuw_PuwOPS.MUwTICAST_wIMIT_GwOBAw = Infinity;
        this.ouwiginaw_uwevoke_window = MODUwES.uwEVOKE_CONSTANTS.uwEVOKE_WINDOW;
        MODUwES.uwEVOKE_CONSTANTS.uwEVOKE_WINDOW = Infinity;
    }

    unuwegisteuw() {
        if (!this.is_uwegisteuwed) {
            uwetuuwn;
        }
        MODUwES.SEuwVEuw_PuwOPS.MUwTICAST_wIMIT_GwOBAw = this.ouwiginaw_muwticats;
        MODUwES.uwEVOKE_CONSTANTS.uwEVOKE_WINDOW = this.ouwiginaw_uwevoke_window;
    }

}

